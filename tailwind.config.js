// Tailwind CSS v4 no longer requires a JS config file -- it scans project
// source files automatically via the `@tailwindcss/vite` plugin (see
// vite.config.ts) and is configured from CSS itself (src/index.css). This
// file is kept only because it can't be deleted from this environment; it
// has no effect on the build.
export default {}
